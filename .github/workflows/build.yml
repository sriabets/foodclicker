name: Build Android APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Check out the project
        uses: actions/checkout@v4

      # Принудительная установка Python 3.11
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Оптимизация кэширования компонентов Android
      - name: Cache Buildozer Android SDK/NDK
        uses: actions/cache@v4
        with:
          path: .buildozer
          key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-

      - name: Cache Pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('buildozer.spec') }}

      # Системные зависимости
      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            git \
            python3-dev \
            ffmpeg \
            libsdl2-dev \
            libsdl2-image-dev \
            libsdl2-mixer-dev \
            libsdl2-ttf-dev \
            libportmidi-dev \
            libswscale-dev \
            libavformat-dev \
            libavcodec-dev \
            zlib1g-dev \
            autoconf \
            automake \
            libtool \
            pkg-config \
            cmake \
            openjdk-17-jdk \
            zip \
            unzip

      # Фиксируем Cython и стабильную версию Buildozer
      - name: Install Buildozer and Cython
        run: |
          python -m pip install --upgrade pip setuptools
          pip install buildozer==1.5.0 cython==0.29.33

      # Запуск быстрой сборки в 4 потока (без OpenSSL она безопасна)
      - name: Build debug APK
        run: |
          set -o pipefail
          yes | buildozer android debug --num-pthreads 4 2>&1 | tee buildozer.log

      # Лог загрузится в любом случае, даже если сборка упадет
      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: buildozer-log
          path: buildozer.log

      # Крок выполнится только при успешной компиляции APK
      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: food-clicker-apk
          path: "**/*.apk"
          if-no-files-found: error
